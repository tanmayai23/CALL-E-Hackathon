{
  "name": "autonomous-incident-escalation",
  "version": "1.0.0",
  "private": true,
  "description": "Phone a human when a system fails, negotiate a commitment, escalate when refused.",
  "scripts": {
    "demo": "CALLE_USE_MOCK=true ts-node scripts/run_escalation.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@call-e/calle": "^0.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
