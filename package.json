{
  "name": "sentinel-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous incident escalation agent — CALL-E Hackathon",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint packages --ext .ts"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@call-e/calle": "^0.7.0",
    "@langchain/core": "^1.2.9",
    "@langchain/langgraph": "^1.4.12"
  }
}
