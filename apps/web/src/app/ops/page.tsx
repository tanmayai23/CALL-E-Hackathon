import { IncidentCommand } from "@/components/ops/IncidentCommand";

export const metadata = { title: "Incident Command — Sentinel Ops" };

export default function OpsPage() {
  return <IncidentCommand />;
}
