# Sentinel Ops — dashboard configuration
#
# Leave NEXT_PUBLIC_API_BASE unset to run against the in-app mock driver: the
# Next route handlers under /api/v1 replay scripted scenarios over the frozen
# SSE contract (CLAUDE.md §8.3). No phone call is placed and no CALL-E credit
# is spent.
#
# Point it at Sameer's backend to run the real path. Nothing else changes —
# every screen talks to the same REST + SSE contract.
#
# NEXT_PUBLIC_API_BASE=https://sentinel-api.up.railway.app
